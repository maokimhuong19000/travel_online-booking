import 'package:flutter/material.dart';
import 'package:travel_online_booking/screens/splash_screens.dart';

void main() {
  runApp(
    const MaterialApp(
      debugShowCheckedModeBanner: false,
      home: SplashScreens(), // Use SplashScreens as the initial screen
    ),
  );
}
